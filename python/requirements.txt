# Python-side dependencies for TestForge.
#
# The AI sidecar and the evaluation harness. The C++ engine needs none of this
# and builds and runs without a Python interpreter present.

# --- AI sidecar -------------------------------------------------------------
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.35
pydantic>=2.6,<3

# The OpenAI SDK is optional: the sidecar starts without it and reports
# key_present/sdk_present=false rather than failing, so `testforge ai status`
# can explain what is missing.
openai>=1.30,<2

# --- evaluation harness and tooling ----------------------------------------
httpx>=0.26,<0.29

# --- development ------------------------------------------------------------
pytest>=8.0,<9
ruff>=0.4,<0.13
